# cc sh

Open the Odoo.sh project page in the browser for the given environment (or the active one).

## Usage

```bash
cc sh              # open SH for the active environment
cc sh myenv        # open SH for a specific environment
```

Requires the environment to have an `sh_url` set. Use `cc env edit` or the web companion's SH sync to configure it.
