# cc project list

List all projects, marking the active one. This is the default verb — bare
`cc project list` runs it.

## Usage

```bash
cc project list
```

## Examples

```bash
cc project list     # show all projects with their active status
```

## Related

- [`cc project create`](create.md) — create a project
- [`cc project env`](env.md) — list a project's environments
- [`cc switch`](../switch.md) — switch the active project
- [Command Reference](../README.md)
